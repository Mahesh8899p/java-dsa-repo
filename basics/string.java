package basics;

public class string {
    String str = "mahesh";
    // str.indexOf(); it takes many pararmeters
    //indexOf(int ch) -> it would return the index of the first occurence
    //indexOf(int ch, int fromIndex) -> it would return the index of the first occurence from fromIndex(fromIndex is the starting index from where it will start counting)
    //indexOf(String str) -> index of the first occurence
    
    
    //str.substring() this method would be taking two parameters 
    //str.substring(int beginingindex, int finalindex)
}
